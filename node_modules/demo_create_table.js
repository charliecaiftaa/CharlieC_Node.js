var mysql = require('mysql');

var con = mysql.createConnection({
    host: "10.11.4.97",
    user: "study",
    password: "Study1111",
    database: "Study"
});

con.connect(function(err) {
    if (err) throw err;
    console.log("Connected!");
    var sql = "CREATE TABLE Charlie_2 (id INT AUTO_INCREMENT PRIMARY KEY, name VARCHAR(255), address VARCHAR(255))";
    con.query(sql, function (err, result) {
        if (err) throw err;
        console.log("Table created");
    });
});

//If there already is a table, create an Alter Table by;  var sql = "ALTER TABLE customers ADD COLUMN id INT AUTO_INCREMENT PRIMARY KEY";